/* eslint-disable no-undef */
import dsa,{a , c, d} from "./module2.mjs"
console.log(dsa);
// eslint-disable-next-line no-undef
console.log(a);
console.log(d);
console.log(c);

// if what we export is not default then we can name it something different, here b is default so we can name it different i.e dsa is given